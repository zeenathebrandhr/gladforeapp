# Copy this file to `.env.local` and fill in the real values before running the app.
# Do NOT commit real keys to git. Keep secrets out of source control.

# Supabase (browser-safe anon key)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Server port the app will listen on (default 5000)
PORT=5000

# Optional: force the Vite HMR client port (useful when running behind GitHub.dev/Codespaces)
# Default is 443 when not set.
VITE_HMR_CLIENT_PORT=443

# Optional: force the HMR host
# VITE_HMR_HOST=weary-incantation-v6j7gjr97wrpfrx-3000.app.github.dev
